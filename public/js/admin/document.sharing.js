(function() {

    // Run a function when the page is fully loaded including graphics.
    document.addEventListener('DOMContentLoaded', () => {

        document.getElementById('documentSharing').addEventListener('click', function(e) {
            //e.preventDefault();
            alert('document');
        });
    });

})();
